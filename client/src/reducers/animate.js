import initialState from '../constants/initialState'

// import {
//   UI_PAINTER_BRUSH_WALL,
// } from '../actions/types'


export default function ui (state = initialState.animate, action) {
  const { 
    type, 
    // payload 
  } = action
  switch (type) {
    default: return state
  }
}
